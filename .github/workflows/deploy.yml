name: CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run simple test
        run: |
          echo "Simulando pruebas"

        

  security-scan:
    runs-on: ubuntu-latest
    needs: build-test
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          ignore-unfixed: true
          format: table
          output: trivy-results.txt
          severity: CRITICAL,HIGH

  validate-and-deploy:
    runs-on: ubuntu-latest
    needs: [security-scan]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Validar políticas de despliegue
        run: |
          if grep -q "CRITICAL" trivy-results.txt; then
            echo "❌ Vulnerabilidades críticas encontradas. Despliegue cancelado."
            exit 1
          else
            echo "✅ Validación superada: No hay vulnerabilidades críticas."
          fi

      - name: Simular despliegue en entorno de pruebas
        run: echo "✔ Desplegado exitosamente en entorno de pruebas"

      - name: Simular despliegue en producción
        run: echo "✔ Desplegado exitosamente en entorno de producción simulado"

      - name: Monitoreo post-despliegue
        run: |
          echo "🟢 Iniciando monitoreo de logs"
          echo "🟢 Todo funciona correctamente. No se detectan anomalías."
